{
  // Процесс генерации
  // * Связи графа определяются гнездами причин
  // 1. А) Определяются фактические вероятности всех ребер.
  // На их основе можно определить, произошло ли каждое из событий
  // в зависимости только от вероятностей, вне зависимости от причин
  // (полагается, что причинные события случились)
  // Б) Для каждой причины определяется массив следствий.
  // Требуется для обхода на этапе 2
  // 2. Отбрасываются непроизошедшие события и их следствия
  // Обход от корневых причин к следствиям.
  // 
  // На этапе 1 определяются (генерируются) недостающие фактические вероятности
  // даже для событий, которые на след. этапе будут помечены неслучившимися,
  // полный обход графа. Зачем обходить узлы, которые не будут в результате?
  // Причина: при анализе причинных гнезд на этапе 2 требуется вся информация
  // о вероятностях с 1-го этапа, а значит этапы нельзя объединить для оптимизации.
  // 
  // Совокупность причин структурируется для определения: произошло событие, или нет.
  // Хранится на стороне следствия.
  // Совокупность следствий удобна для обхода от корней и как альтернатива
  // функциональности групп (но не замена).
  // Хранится на стороне причины.

  "roots": [
    // Id корневых узлов. Каждый корневой узел имеет также имеет гнезда причин
    "id",
    "id1",
    "id2"
  ],
  "nodes": [
    {
      // Node
      "id": "node id",
      "nodeValue": {
        // Some object
      },
      "causes": [
        {
          // Causes nest
          "edges": [
            {
              // Edge
              "probability": 0.6,
              "actualProbability": 0.3,
              "causeId": "cause id" // => to next node
            }
          ]
        }
      ],
      "groupId": "group id",
      "isHappened": true
    }
  ]
}